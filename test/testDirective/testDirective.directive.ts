import { DirectiveOptions } from 'vue/types/umd'


const testDirectiveDirective: DirectiveOptions = {
  inserted(el: HTMLElement) {
  },
  bind(el: HTMLElement) {
    
  },
  unbind(el:HTMLElement) {
   
  }
}

export default testDirective;